<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login | LAP CART</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background-color: #1a1a1a;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .container {
            display: flex;
            width: 90%;
            max-width: 900px;
            height: 500px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
            border-radius: 8px;
            overflow: hidden;
        }
        
        .brand-section {
            background-color: #000;
            width: 40%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        .logo-container {
            text-align: center;
        }
        
        .logo {
            width: 100px;
            margin-bottom: 15px;
        }
        
        .brand-name {
            font-size: 28px;
            font-weight: 700;
            letter-spacing: 1px;
        }
        
        .brand-name .red {
            color: #FF0000;
        }
        
        .brand-name .white {
            color: #FFFFFF;
        }
        
        .login-section {
            background-color: #21252b;
            width: 60%;
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        
        .login-header {
            margin-bottom: 30px;
        }
        
        .login-header h2 {
            color: #fff;
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 5px;
        }
        
        .login-header p {
            color: #aaa;
            font-size: 14px;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            font-size: 14px;
            margin-bottom: 8px;
            color: #ddd;
        }
        
        .form-group input {
            width: 100%;
            padding: 12px;
            border: none;
            background-color: #2c3138;
            color: #fff;
            border-radius: 4px;
            font-size: 15px;
            outline: none;
            transition: all 0.3s;
        }
        
        .form-group input:focus {
            box-shadow: 0 0 0 2px rgba(138, 43, 226, 0.3);
        }
        
        .remember-me {
            display: flex;
            align-items: center;
            margin-bottom: 25px;
        }
        
        .remember-me input {
            margin-right: 8px;
        }
        
        .remember-me label {
            color: #ddd;
            font-size: 14px;
        }
        
        .btn-login {
            background-color: #8A2BE2;
            color: white;
            border: none;
            padding: 12px;
            width: 100%;
            border-radius: 4px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        
        .btn-login:hover {
            background-color: #7B27CA;
        }
        
        .error-message {
            color: #ff4d4d;
            font-size: 14px;
            margin-top: 20px;
            text-align: center;
        }
        
        /* Responsive styles */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                height: auto;
            }
            
            .brand-section {
                width: 100%;
                padding: 30px 20px;
            }
            
            .login-section {
                width: 100%;
                padding: 30px 20px;
            }
        }
        
        @media (max-width: 480px) {
            .container {
                width: 95%;
            }
            
            .login-header h2 {
                font-size: 20px;
            }
            
            .form-group input {
                padding: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="brand-section">
            <div class="logo-container">
                <img src="/images/logo.png" alt="LAP CART Logo" class="logo">
                <h1 class="brand-name">
                    <span class="red">LAP</span> <span class="white">CART</span>
                </h1>
            </div>
        </div>
        <div class="login-section">
            <div class="login-header">
                <h2>Admin Login</h2>
                <p>Please enter your credentials to access the admin panel</p>
            </div>
            
            <form action="/admin/login" method="POST">
                <% if(locals.message) { %>
                <div class="error-message">
                    <%= message %>
                </div>
                <% } %>
                
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" placeholder="Enter your email" required>
                </div>
                
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="Enter your password" required>
                </div>
                
                <div class="remember-me">
                    <input type="checkbox" id="remember" name="remember">
                    <label for="remember">Remember me</label>
                </div>
                
                <button type="submit" class="btn-login">Login</button>
            </form>
        </div>
    </div>
</body>
</html>